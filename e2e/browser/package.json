{
  "name": "e2e-browser",
  "type": "module",
  "scripts": {
    "start": "npx @newmo/graphql-fake-server --port 4000 --schema ./1-basic-schema.graphql --logLevel=debug",
    "gen": "graphql-codegen-esm",
    "lint": "run-s -c lint:*",
    "lint:tsc": "tsc",
    "test": "vitest --run"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@newmo/graphql-fake-server": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^1.3.1",
    "vitest-github-actions-reporter": "^0.11.1"
  }
}
