{
    "private": true,
    "name": "e2e-browser",
    "version": "0.3.1",
    "type": "module",
    "scripts": {
        "start": "npm exec @newmo/graphql-fake-server -- --schema ./1-basic-schema.graphql --logLevel=debug",
        "lint": "run-s -c lint:*",
        "lint:tsc": "tsc",
        "test": "vitest --run"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "@newmo/graphql-fake-server": "workspace:*"
    },
    "devDependencies": {
        "@tsconfig/node18": "^18.2.0",
        "@tsconfig/strictest": "^2.0.1",
        "vitest": "^1.3.1",
        "vitest-github-actions-reporter": "^0.11.1"
    }
}
